---
name: daa-stock-research
description: A股每日投研系统。每天生成市场温度+仓位建议+1-2只候选股（评分/买点/止损/止盈/可展期评估），推荐股进入跟踪池做连续跟踪（收盘体检/展期评估/止盈止损触发），支持微信/企业微信推送。适用于需要A股短线波段(1-2周)每日投研工作流、个人交易风控纪律执行的场景。当用户提到"每日投研/荐股/选股/仓位建议/跟踪池/展期/止盈止损/收盘复盘/午间复盘/盘中监控"等时使用。
---

# 大A每日投研系统 (daa-stock-research)

A股个人投资者的每日投研自动化系统：**晨报荐股 → 跟踪池连续跟踪 → 展期/止盈/止损生命周期管理**，数据源为 akshare（免费）。

## 工作流总览

```
09:25 盘前  daily_report.py  晨报：市场温度+仓位建议+候选股(评分/买点/止损/止盈/可展期分) → 微信推送 + 台账落账
11:35 午间  noon_review.py   午间复盘：持仓/跟踪池半日表现
15:10 收盘  close_review.py  收盘复盘：台账状态更新(状态机)+跟踪池体检+次日关注
盘中    15min monitor.py     盘中监控：买区/止损/止盈触发告警 → 微信推送
```

## 核心概念

### 推荐池生命周期（2026-09 重构，取代旧持仓管理）

- 每日荐股 **1-2 只**（质量不足可 0 只），入选即写入 `data/推荐台账.csv`
- 台账状态机：`持有中 → 止盈1减半 → (止盈2清仓|止损出局|到期离场)`，另有 `展期中`
- **满 10 天触发展期评估**（小概率转中长期）：盈利>0、峰值回撤≤6%、价>MA10>MA20、量能≥60%、名额<2 才可展期
- **展期硬约束**：同时 ≤2 只（`EXTEND_MAX_COUNT`）、最长 30 天（`EXTEND_MAX_DAYS`）强制离场
- **双档止盈**：波段票 +6%减半/+10%清仓；展期票切换 +10%减半/+15%清仓（均基于**成本价**，到了就走）

### 选股引擎（右侧追启动）

- 评分体系：价值30 + 技术30 + 资金25 + 题材15 = 100，≥70 推荐 / ≥80 强烈推荐
- **右侧追启动**：放量(量比>1.3)突破20日新高 +3 分
- **涨停剔除**：现价/涨停价 ≥0.95（近涨停）或 5 日内连板≥2 → 剔除（解决"好看不好买"）
- 行业去重：同行业最多 1 只（`MAX_SAME_INDUSTRY`）
- **可展期分 0-20**（独立维度，不计入主评分）：中期趋势/行业强势/估值/盈利/量能/位置，≥12 标 🟢 可展期，否则 🟡 纯波段
- 代码白名单：主板(60/00)+创业板(30)；排除科创板(68)/北交所(92/43/83)——无交易权限

## 使用方式

### 1. 安装依赖

```bash
pip install -r requirements.txt
# 建议：pip install akshare pandas numpy requests
```

### 2. 配置（可选）

- **数据目录**：首次运行自动在包根创建 `data/`、`05_每日推送/`、`04_每日复盘/`
- **项目根**：默认包根；可用环境变量覆盖 `DAA_PROJECT_ROOT`
- **微信推送**：创建 `data/push_config.json`（未配置则推送静默跳过）：
  ```json
  {"pushplus_token": "你的pushplus令牌"}
  ```
- **价值投资交叉验证**（可选增强）：安装 stock-researcher skill 后自动启用；未安装时该板块自动降级为空。可用 `DAA_SKILL_DIR` 指定其路径。

### 3. 运行

```bash
cd <skill根目录>
python scripts/daily_report.py    # 晨报：生成 05_每日推送/推送_YYYY-MM-DD.md 并推送
python scripts/noon_review.py     # 午间复盘
python scripts/close_review.py    # 收盘复盘（15:10 后）
python scripts/monitor.py         # 盘中监控（配合定时任务每15分钟）
```

> 本机生产环境示例：`DAA_PROJECT_ROOT=E:\workbuddy——大A python scripts/daily_report.py`

### 4. 定时化建议（工作日）

- 09:25 `daily_report.py`（晨报推送）
- 11:35 `noon_review.py`
- 15:10 `close_review.py`
- 盘中 09:35-15:00 每15分钟 `monitor.py`

## 风控参数速查

| 参数 | 值 | 含义 |
|------|----|----|
| `STOP_LOSS` | -6% | 止损线（基于成本价） |
| `TAKE_PROFIT_1/2` | +6%/+10% | 波段双档止盈（减半/清仓） |
| `EXTEND_TP1/2` | +10%/+15% | 展期后双档止盈（减半/清仓） |
| `HOLD_DAYS_MAX` | 10天 | 满10天触发展期评估 |
| `EXTEND_MAX_DAYS` | 30天 | 展期硬上限 |
| `EXTEND_MAX_COUNT` | 2只 | 同时展期上限 |
| `MAX_POSITION_PER_STOCK` | 30% | 单票最大仓位 |

> 风控参数集中在 `scripts/config.py`，为全系统唯一来源。

## 输出物

- `data/推荐台账.csv` — 推荐票生命周期台账
- `data/today_picks.json` — 当日推荐（供 monitor 读取）
- `05_每日推送/` — 晨报/午间/复盘存档
- `04_每日复盘/` — 收盘复盘存档

## 免责声明

本项目仅供个人学习研究，不构成任何投资建议。股市有风险，入市需谨慎。
